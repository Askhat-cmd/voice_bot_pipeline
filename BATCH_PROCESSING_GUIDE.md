# 📦 **РУКОВОДСТВО ПО ПАКЕТНОЙ ОБРАБОТКЕ**

## 🎯 **ЦЕЛЬ**
Создать тестовую коллекцию из **50 обработанных JSON файлов** с канала Саламата Сарсекенова для тестирования векторного поиска и SAG v2.0 системы.

## 📁 **СТРУКТУРА ФАЙЛОВ**

### **Входные данные:**
- `urls.txt` - файл с YouTube URL-ами для обработки
- `test_urls_batch.txt` - дополнительный файл с URL-ами

### **Обработка:**
- `pipeline_orchestrator.py` - основной Python скрипт (уже работает!)

### **Результаты:**
- `data/sag_final/*.for_vector.json` - обработанные SAG v2.0 файлы
- `data/raw_subtitles/batch_pipeline_results_*.json` - логи пакетной обработки

---

## 🚀 **БЫСТРЫЙ СТАРТ**

### **1. Подготовка URL-ов**
Отредактируйте файл `urls.txt` или создайте `test_urls_batch.txt`:
```txt
# Добавьте 50 URL-ов с канала Сарсекенова
https://www.youtube.com/watch?v=VIDEO_ID_1
https://www.youtube.com/watch?v=VIDEO_ID_2
# ... и так далее
```

### **2. Запуск обработки**
```powershell
# Обработать файл с URL-ами
python pipeline_orchestrator.py --urls-file urls.txt --config config.yaml

# Или обработать один URL
python pipeline_orchestrator.py --url "https://www.youtube.com/watch?v=VIDEO_ID" --config config.yaml
```

### **3. Проверка результатов**
```powershell
# Посмотреть количество обработанных файлов
(Get-ChildItem data/sag_final/*.for_vector.json).Count

# Посмотреть список файлов
Get-ChildItem data/sag_final/*.for_vector.json | Select-Object Name
```

---

## ⚙️ **ДЕТАЛЬНАЯ НАСТРОЙКА**

### **Параметры запуска:**
```powershell
# Базовый запуск
.\scripts\run_batch_processing.ps1

# С кастомным файлом URL-ов
.\scripts\run_batch_processing.ps1 -UrlsFile my_urls.txt

# С несколькими параллельными процессами
.\scripts\run_batch_processing.ps1 -MaxWorkers 2

# Показать справку
.\scripts\run_batch_processing.ps1 -Help
```

### **Прямой запуск Python:**
```bash
# Базовый запуск
python batch_processor.py

# С параметрами
python batch_processor.py --urls-file test_urls_batch.txt --max-workers 1
```

---

## 📊 **МОНИТОРИНГ ПРОЦЕССА**

### **Логи в реальном времени:**
```powershell
# Следить за логами
Get-Content data/raw_subtitles/pipeline.log -Wait -Tail 10
```

### **Статистика обработки:**
- **Всего URL-ов**: количество загруженных URL-ов
- **Обработано**: количество обработанных URL-ов
- **Успешно**: количество успешно обработанных
- **Ошибок**: количество ошибок
- **Пропущено**: количество уже обработанных файлов

### **Файлы результатов:**
- `data/sag_final/*.for_vector.json` - SAG v2.0 JSON файлы
- `data/sag_final/*.for_review.md` - Markdown файлы для проверки
- `data/raw_subtitles/batch_results_*.json` - детальные логи

---

## 🔧 **УСТРАНЕНИЕ ПРОБЛЕМ**

### **Частые ошибки:**

#### **1. "Файл с URL-ами не найден"**
```powershell
# Проверьте существование файла
Test-Path test_urls_batch.txt

# Создайте файл с URL-ами
New-Item test_urls_batch.txt -ItemType File
```

#### **2. "Python не найден"**
```powershell
# Проверьте установку Python
python --version

# Установите Python 3.8+ и добавьте в PATH
```

#### **3. "Не все зависимости установлены"**
```powershell
# Установите зависимости
pip install -r requirements.txt
```

#### **4. "Ошибка обработки URL"**
- Проверьте корректность URL-ов
- Убедитесь, что видео доступно
- Проверьте логи в `data/raw_subtitles/pipeline.log`

---

## 📈 **ОПТИМИЗАЦИЯ ПРОИЗВОДИТЕЛЬНОСТИ**

### **Рекомендации:**
1. **Параллельная обработка**: используйте `-MaxWorkers 2-3` для ускорения
2. **Пакетный размер**: обрабатывайте по 10-20 URL-ов за раз
3. **Мониторинг ресурсов**: следите за использованием CPU и памяти
4. **Проверка результатов**: регулярно проверяйте качество обработки

### **Ограничения:**
- **YouTube API**: соблюдайте лимиты запросов
- **OpenAI API**: следите за лимитами токенов
- **Дисковое пространство**: каждый файл ~50-100KB

---

## 🎯 **СЛЕДУЮЩИЕ ШАГИ**

После успешной пакетной обработки:

1. **✅ Проверьте результаты** - убедитесь, что все 50 файлов созданы
2. **🔍 Анализ качества** - проверьте несколько файлов вручную
3. **🧪 Тестирование** - запустите тесты векторного поиска
4. **📊 Статистика** - проанализируйте метрики обработки
5. **🚀 Масштабирование** - подготовьте к обработке большего объема

---

## 📞 **ПОДДЕРЖКА**

При возникновении проблем:
1. Проверьте логи в `data/raw_subtitles/pipeline.log`
2. Убедитесь в корректности URL-ов
3. Проверьте доступность API (YouTube, OpenAI)
4. Обратитесь к документации проекта

**Удачной обработки! 🚀**
