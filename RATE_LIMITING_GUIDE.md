# Руководство по Rate Limiting для эмбеддингов

## Обзор

Система была перенастроена для работы с ограниченной квотой OpenAI API. Теперь все запросы к API эмбеддингов автоматически разбиваются на чанки с задержками и retry логикой.

## Настройки в config.yaml

```yaml
vector_db:
  rate_limiting:
    # Максимальный размер чанка для одного запроса к API
    # Рекомендуется: 10-50 для ограниченной квоты
    chunk_size: 10
    
    # Задержка между запросами в секундах
    # Помогает избежать rate limit ошибок
    delay_between_requests: 0.5
    
    # Максимальное количество попыток при ошибке rate limit
    max_retries: 5
    
    # Начальная задержка при retry в секундах
    # Увеличивается экспоненциально: retry_delay * (2 ^ attempt)
    retry_delay: 2
    
    # Максимальная задержка при retry в секундах
    max_retry_delay: 60
```

## Как это работает

### 1. Чанкинг (Chunking)
- Большие батчи автоматически разбиваются на чанки размером `chunk_size`
- Каждый чанк обрабатывается отдельным запросом к API
- Пример: 100 текстов с `chunk_size: 10` → 10 запросов по 10 текстов

### 2. Rate Limiting
- Между каждым запросом есть задержка `delay_between_requests`
- Помогает избежать превышения rate limit
- Пример: при `delay_between_requests: 0.5` между запросами будет пауза 0.5 секунды

### 3. Retry логика
- При ошибке 429 (rate limit) система автоматически повторяет запрос
- Задержка между попытками увеличивается экспоненциально:
  - Попытка 1: 2 секунды
  - Попытка 2: 4 секунды
  - Попытка 3: 8 секунд
  - И так далее до `max_retry_delay`
- Максимум `max_retries` попыток

### 4. Обработка ошибок
- **429 (Rate Limit)**: Автоматический retry с экспоненциальной задержкой
- **429 (Insufficient Quota)**: Ошибка с ссылкой на биллинг
- **401 (Invalid API Key)**: Ошибка с указанием проверить ключ
- **Другие ошибки**: Логируются с полной информацией

## Примеры использования

### Автоматическая индексация через pipeline

```powershell
# Индексация произойдет автоматически после обработки
python pipeline_orchestrator.py --url "https://youtu.be/VIDEO_ID" --config config.yaml
```

Система автоматически:
1. Разобьет все тексты на чанки
2. Будет делать паузы между запросами
3. Повторит запросы при rate limit ошибках

### Индексация существующих файлов

```powershell
# Индексация с rate limiting
python scripts/index_existing_files.py --config config.yaml
```

### Тестирование rate limiting

```powershell
# Тест системы rate limiting
python test_rate_limiting.py
```

## Рекомендации по настройке

### Для очень ограниченной квоты:
```yaml
rate_limiting:
  chunk_size: 5              # Маленькие чанки
  delay_between_requests: 1.0  # Большие задержки
  max_retries: 10            # Больше попыток
  retry_delay: 5.0           # Больше начальная задержка
```

### Для умеренной квоты:
```yaml
rate_limiting:
  chunk_size: 10             # Средние чанки
  delay_between_requests: 0.5  # Средние задержки
  max_retries: 5             # Стандартные попытки
  retry_delay: 2.0           # Стандартная задержка
```

### Для хорошей квоты:
```yaml
rate_limiting:
  chunk_size: 50             # Большие чанки
  delay_between_requests: 0.1  # Маленькие задержки
  max_retries: 3             # Меньше попыток
  retry_delay: 1.0           # Меньше задержка
```

## Мониторинг

В логах вы увидите:
```
📦 Обработка 100 текстов в 10 чанках (размер чанка: 10)
  Чанк 1/10: обработка 10 текстов...
  ✅ Чанк 1/10 обработан успешно
  Чанк 2/10: обработка 10 текстов...
  ✅ Чанк 2/10 обработан успешно
  ...
✅ Все 100 текстов успешно обработаны
```

При rate limit:
```
⚠️ Rate limit достигнут. Попытка 1/5. Ожидание 2.0s...
⚠️ Rate limit достигнут. Попытка 2/5. Ожидание 4.0s...
✅ Чанк обработан успешно
```

## Устранение неполадок

### Проблема: Все еще получаю ошибки 429
**Решение**: Увеличьте `delay_between_requests` и уменьшите `chunk_size`

### Проблема: Индексация слишком медленная
**Решение**: Уменьшите `delay_between_requests` и увеличьте `chunk_size` (если позволяет квота)

### Проблема: Ошибка "Insufficient Quota"
**Решение**: Проверьте биллинг на https://platform.openai.com/account/billing

## Технические детали

- Чанкинг работает на уровне `EmbeddingService.create_embeddings_batch()`
- Retry логика реализована в `_make_request_with_retry()`
- Все настройки загружаются из `config.yaml`
- Система полностью обратно совместима (работает с дефолтными значениями)



