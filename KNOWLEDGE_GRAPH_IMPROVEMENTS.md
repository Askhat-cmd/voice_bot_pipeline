# 🚀 Улучшения Knowledge Graph для максимально полной базы знаний

## 📋 Обзор

Реализована система построения максимально полного Knowledge Graph, объединяющая все данные из экстракторов SAG v2.0 для создания базы знаний с максимальным контекстом и взаимосвязями для AI-бота психолога.

## ✅ Что было реализовано

### 1. Метод `_build_knowledge_graph_from_blocks()`

Объединяет данные из всех источников:

- **graph_entities** (rule-based извлечение) - 442 узла + 259 отношений
- **concept_hierarchy** (иерархия концептов) - структурированные концепты
- **causal_chains** (причинно-следственные цепочки) - процессы и этапы
- **prerequisites** (предпосылки) - последовательности обучения
- **case_studies** (кейсы) - примеры и ситуации
- **semantic_relationships** (семантические связи) - автоматические связи
- **cooccurrence** (совместные появления) - перекрестные связи между блоками

### 2. Типизация узлов и связей

**Типы узлов:**
- `CONCEPT` - концепты и понятия
- `PRACTICE` - практики
- `TECHNIQUE` - техники
- `EXERCISE` - упражнения
- `PATTERN` - паттерны
- `PROCESS_STAGE` - этапы процессов

**Типы связей:**
- `IS_CORE_COMPONENT_OF` - иерархические связи
- `IS_PRACTICE_FOR` - практика для концепта
- `IS_TECHNIQUE_FOR` - техника для практики
- `IS_EXERCISE_FOR` - упражнение для техники
- `ENABLES` - позволяет/включает
- `REQUIRES` - требует/предпосылка
- `LEADS_TO` - ведет к
- `TRANSFORMS_INTO` - трансформируется в
- `EMERGES_FROM` - возникает из
- `RELATED_TO` - связан с

### 3. API методы для AI-бота

#### `find_practices_for_concept(concept: str, knowledge_graph: Dict) -> List[Dict]`
Находит практики для работы с концептом/симптомом.

**Пример:**
```python
practices = processor.find_practices_for_concept("обида", knowledge_graph)
# → [{"practice": "метанаблюдение", "relation": "ENABLES", ...}]
```

#### `get_concept_chain(from_concept: str, to_concept: str, knowledge_graph: Dict) -> Optional[List[Dict]]`
Получает цепочку связей между двумя концептами (BFS поиск пути).

**Пример:**
```python
chain = processor.get_concept_chain("страдание", "освобождение", knowledge_graph)
# → [{"from": "страдание", "to": "осознавание", "relation": "LEADS_TO", ...}, ...]
```

#### `recommend_exercise_for_practice(practice: str, knowledge_graph: Dict, duration: Optional[str]) -> Optional[Dict]`
Рекомендует упражнение для практики.

**Пример:**
```python
exercise = processor.recommend_exercise_for_practice("метанаблюдение", knowledge_graph, "5 минут")
# → {"exercise": "наблюдение за дыханием", "duration": "5-10 минут", ...}
```

### 4. Индексация в Vector DB

Добавлен метод `index_knowledge_graph()` в `VectorIndexer`:

- Индексирует все узлы графа с контекстом связей
- Сохраняет метаданные (тип узла, количество связей, источники)
- Автоматически включается в `index_sag_file()` при наличии `knowledge_graph` в данных

**Конфигурация:**
```yaml
vector_db:
  index_levels:
    - "documents"
    - "blocks"
    - "graph_entities"
    - "knowledge_graph"  # 🚀 Новый уровень
```

## 📊 Структура Knowledge Graph

```json
{
  "knowledge_graph": {
    "nodes": [
      {
        "id": "node_0001",
        "name": "метанаблюдение",
        "node_type": "PRACTICE",
        "description": "Наблюдение за наблюдающим",
        "metadata": {
          "frequency": 5,
          "source": "graph_entities",
          "level": "practice"
        }
      }
    ],
    "edges": [
      {
        "from_id": "node_0001",
        "to_id": "node_0002",
        "from_name": "метанаблюдение",
        "to_name": "осознавание",
        "edge_type": "ENABLES",
        "explanation": "Метанаблюдение ведет к осознаванию",
        "confidence": 0.9,
        "metadata": {
          "source": "causal_chains"
        }
      }
    ],
    "metadata": {
      "video_id": "...",
      "total_nodes": 45,
      "total_edges": 78,
      "node_types": {
        "CONCEPT": 20,
        "PRACTICE": 10,
        "TECHNIQUE": 8,
        "EXERCISE": 5,
        "PROCESS_STAGE": 2
      },
      "edge_types": {
        "ENABLES": 15,
        "REQUIRES": 12,
        "LEADS_TO": 20,
        "RELATED_TO": 25,
        ...
      },
      "sources": {
        "graph_entities": 20,
        "concept_hierarchy": 10,
        "causal_chains": 8,
        "prerequisites": 5,
        "case_studies": 2
      }
    }
  }
}
```

## 🎯 Преимущества

1. **Максимальная полнота**: Объединение всех источников данных
2. **Типизированные связи**: Четкая структура узлов и отношений
3. **Контекст**: Каждая связь имеет объяснение и уверенность
4. **API для бота**: Готовые методы для поиска и рекомендаций
5. **Векторный поиск**: Индексация в ChromaDB для семантического поиска
6. **Отслеживание источников**: Метаданные показывают откуда извлечен каждый узел

## 🔄 Интеграция

Knowledge Graph автоматически строится при обработке каждого видео:

1. Обработка блоков экстракторами SAG v2.0
2. Извлечение граф-сущностей (rule-based)
3. Построение Knowledge Graph из всех данных
4. Сохранение в JSON файл
5. Индексация в Vector DB

## 📝 Использование

### В коде процессора:
```python
processor = SarsekenovProcessor()
result = processor.process_file(input_path, output_dir)

# Knowledge Graph доступен в result
knowledge_graph = result.get("knowledge_graph")
```

### Для AI-бота:
```python
# Загрузить knowledge_graph из JSON
with open("data/sag_final/.../video.for_vector.json") as f:
    data = json.load(f)
    kg = data["knowledge_graph"]

# Использовать API методы
practices = processor.find_practices_for_concept("обида", kg)
chain = processor.get_concept_chain("страдание", "освобождение", kg)
exercise = processor.recommend_exercise_for_practice("метанаблюдение", kg)
```

## 🚀 Результат

Теперь каждый обработанный видео содержит:

- ✅ Полный Knowledge Graph со всеми взаимосвязями
- ✅ Типизированные узлы и связи
- ✅ Контекст для каждого узла
- ✅ Готовые API методы для AI-бота
- ✅ Индексация в Vector DB для семантического поиска

**Максимально полная база знаний для супер-умного AI-бота психолога!** 🧠✨

