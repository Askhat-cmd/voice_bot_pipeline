# 🧠 Knowledge Graph - Максимально полная база знаний

## Навигация

- [Назад к README](../README.md)
- [SAG v2.0 Обзор](./sag-v2-overview.md)
- [Система экстракторов](./extractors-system.md)
- [API и интеграция](./api-integration.md)

---

## 📖 Описание и назначение

**Назначение документа**: Это полная документация по системе Knowledge Graph - максимально полной базе знаний, которая объединяет все данные из экстракторов SAG v2.0. Документ содержит информацию о структуре графа, типах узлов и связей, API методах для AI-бота и интеграции.

**Для кого**: Документ предназначен для разработчиков AI-ботов, которые хотят использовать Knowledge Graph для поиска практик, рекомендаций упражнений и построения reasoning chains. Также полезен для архитекторов, планирующих интеграцию графа знаний в свои системы.

**Что содержит**:
- Обзор системы Knowledge Graph (что объединяет, источники данных)
- Типизация узлов и связей (все типы с примерами: CONCEPT, PRACTICE, TECHNIQUE, EXERCISE, PATTERN, PROCESS_STAGE)
- Типы связей (10+ типов: ENABLES, REQUIRES, LEADS_TO, IS_PRACTICE_FOR и др.)
- Вычисление весов связей (PMI, co-occurrence, distance - полное описание методов)
- API методы для AI-бота (полные примеры кода: find_practices_for_concept, get_concept_chain, recommend_exercise)
- Индексация в Vector DB (как граф индексируется для семантического поиска)
- Структура Knowledge Graph (полная схема данных, формат JSON с весами)
- Файлы Knowledge Graph (все форматы сохранения, структура файлов)
- Использование Knowledge Graph (примеры интеграции, best practices, работа с весами)

**Когда использовать**: Используйте этот документ при разработке AI-бота, который должен использовать граф знаний для поиска и рекомендаций, при интеграции Knowledge Graph в существующие системы, или при изучении структуры данных графа.

---

## Обзор

Реализована система построения максимально полного Knowledge Graph, объединяющая все данные из экстракторов SAG v2.0 для создания базы знаний с максимальным контекстом и взаимосвязями для AI-бота психолога.

---

## Что объединяет Knowledge Graph

Knowledge Graph объединяет данные из всех источников:

1. **graph_entities** (rule-based извлечение) - 442 узла + 259 отношений
2. **concept_hierarchy** (иерархия концептов) - структурированные концепты
3. **causal_chains** (причинно-следственные цепочки) - процессы и этапы
4. **prerequisites** (предпосылки) - последовательности обучения
5. **case_studies** (кейсы) - примеры и ситуации
6. **semantic_relationships** (семантические связи) - автоматические связи
7. **cooccurrence** (совместные появления) - перекрестные связи между блоками

---

## Типизация узлов и связей

### Типы узлов

- `CONCEPT` - концепты и понятия
- `PRACTICE` - практики
- `TECHNIQUE` - техники
- `EXERCISE` - упражнения
- `PATTERN` - паттерны
- `PROCESS_STAGE` - этапы процессов

### Типы связей

#### Иерархические (вертикальные)
- `IS_CORE_COMPONENT_OF` - иерархические связи
- `IS_PRACTICE_FOR` - практика для концепта
- `IS_TECHNIQUE_FOR` - техника для практики
- `IS_EXERCISE_FOR` - упражнение для техники

#### Процессуальные
- `EMERGES_FROM` - возникает из
- `ENABLES` - позволяет/включает
- `REQUIRES` - требует/предпосылка
- `LEADS_TO` - ведет к
- `TRANSFORMS_INTO` - трансформируется в

#### Паттерны
- `RELATED_TO` - связан с
- `PART_OF_PATTERN` - часть паттерна

---

## API методы для AI-бота

### `find_practices_for_concept(concept: str, knowledge_graph: Dict) -> List[Dict]`

Находит практики для работы с концептом/симптомом.

**Пример:**
```python
practices = processor.find_practices_for_concept("обида", knowledge_graph)
# → [{"practice": "метанаблюдение", "relation": "ENABLES", ...}]
```

### `get_concept_chain(from_concept: str, to_concept: str, knowledge_graph: Dict) -> Optional[List[Dict]]`

Получает цепочку связей между двумя концептами (BFS поиск пути).

**Пример:**
```python
chain = processor.get_concept_chain("страдание", "освобождение", knowledge_graph)
# → [{"from": "страдание", "to": "осознавание", "relation": "LEADS_TO", ...}, ...]
```

### `recommend_exercise_for_practice(practice: str, knowledge_graph: Dict, duration: Optional[str]) -> Optional[Dict]`

Рекомендует упражнение для практики.

**Пример:**
```python
exercise = processor.recommend_exercise_for_practice("метанаблюдение", knowledge_graph, "5 минут")
# → {"exercise": "наблюдение за дыханием", "duration": "5-10 минут", ...}
```

---

## Индексация в Vector DB

Knowledge Graph автоматически индексируется в отдельную коллекцию ChromaDB (`knowledge_graph`):

- Каждый узел индексируется с контекстом его связей
- Метаданные включают тип узла, количество связей, источники извлечения
- Доступен для семантического поиска вместе с другими коллекциями

---

## Вычисление весов связей

**🚀 НОВОЕ:** Система автоматически вычисляет веса связей между концептами вместо фиксированного значения `confidence=1.0`.

### Методы вычисления весов

Система использует комбинированный подход для расчета весов связей:

1. **Co-occurrence frequency** (40% веса)
   - Частота совместной встречаемости концептов в блоках
   - Нормализуется относительно максимальной частоты
   - Высокая частота → высокий вес

2. **PMI (Pointwise Mutual Information)** (30% веса)
   - Статистическая мера взаимосвязи концептов
   - Показывает, насколько чаще концепты встречаются вместе, чем случайно
   - Использует сигмоидную нормализацию для диапазона [0, 1]

3. **Distance weight** (30% веса)
   - Близость концептов в тексте (расстояние в словах)
   - Чем ближе концепты в тексте, тем выше вес
   - Использует экспоненциальное затухание: `exp(-distance / 50)`

### Формула комбинированного веса

```
combined_weight = 0.4 * freq_weight + 0.3 * pmi_weight + 0.3 * dist_weight
```

Где:
- `freq_weight` - нормализованная частота co-occurrence [0, 1]
- `pmi_weight` - нормализованный PMI [0, 1]
- `dist_weight` - вес на основе расстояния [0, 1]

Итоговый вес ограничен диапазоном **[0.1, 1.0]**.

### Результаты вычисления

- **Веса варьируются** от 0.1 до 1.0 (вместо фиксированного 1.0)
- **Часто встречающиеся вместе концепты** имеют вес > 0.7
- **Редко связанные концепты** имеют вес < 0.3
- **Статистика весов** сохраняется в `metadata.weight_statistics`:
  - `min_weight` - минимальный вес среди всех связей
  - `max_weight` - максимальный вес среди всех связей
  - `avg_weight` - средний вес всех связей
  - `median_weight` - медианный вес всех связей

### Преимущества вычисляемых весов

- ✅ **Приоритизация связей** для RAG-системы - более релевантные связи имеют больший вес
- ✅ **Улучшение качества поиска** - система может фильтровать слабые связи
- ✅ **Статистический анализ** - метрики показывают распределение весов в графе
- ✅ **Адаптивность** - веса автоматически адаптируются к содержимому документа

### Пример использования весов

```python
# Загрузить Knowledge Graph
with open("video.for_vector.json", "r", encoding="utf-8") as f:
    data = json.load(f)
    kg = data["knowledge_graph"]

# Получить статистику весов
weight_stats = kg["metadata"]["weight_statistics"]
print(f"Диапазон весов: {weight_stats['min_weight']:.3f} - {weight_stats['max_weight']:.3f}")
print(f"Средний вес: {weight_stats['avg_weight']:.3f}")

# Фильтровать связи по весу
strong_edges = [
    edge for edge in kg["edges"] 
    if edge.get("confidence", 1.0) > 0.5
]
print(f"Сильных связей (>0.5): {len(strong_edges)}")

# Сортировать связи по весу
sorted_edges = sorted(
    kg["edges"], 
    key=lambda e: e.get("confidence", 1.0), 
    reverse=True
)
print("Топ-5 самых сильных связей:")
for edge in sorted_edges[:5]:
    print(f"  {edge['from_name']} → {edge['to_name']}: {edge.get('confidence', 1.0):.3f}")
```

## Преимущества

1. **Максимальная полнота**: Объединение всех источников данных
2. **Типизированные связи**: Четкая структура узлов и отношений
3. **Вычисляемые веса**: Автоматический расчет весов связей на основе статистики
4. **Контекст**: Каждая связь имеет объяснение и уверенность (вес)
5. **API для бота**: Готовые методы для поиска и рекомендаций
6. **Векторный поиск**: Индексация в ChromaDB для семантического поиска
7. **Отслеживание источников**: Метаданные показывают откуда извлечен каждый узел
8. **Статистика весов**: Метаданные содержат полную статистику распределения весов

---

## Структура Knowledge Graph

Knowledge Graph сохраняется в двух местах:

1. **В основном JSON файле** (`*.for_vector.json`) в поле `knowledge_graph`
2. **В отдельном файле** (`*.knowledge_graph.json`) для удобного доступа

### Пример структуры Knowledge Graph

```json
{
  "knowledge_graph": {
    "nodes": [
      {
        "id": "node_0001",
        "name": "метанаблюдение",
        "node_type": "PRACTICE",
        "description": "Наблюдение за наблюдающим",
        "sarsekenov_terms": ["метанаблюдение", "наблюдение за наблюдающим"],
        "tier": 3,
        "confidence": 1.0,
        "metadata": {
          "frequency": 5,
          "source": "graph_entities",
          "level": "practice"
        }
      },
      {
        "id": "node_0002",
        "name": "осознавание",
        "node_type": "CONCEPT",
        "description": "Процесс осознания происходящего",
        "sarsekenov_terms": ["осознавание", "осознание"],
        "tier": 2,
        "confidence": 1.0,
        "metadata": {
          "source": "concept_hierarchy",
          "level": "domain"
        }
      }
    ],
    "edges": [
      {
        "from_id": "node_0001",
        "to_id": "node_0002",
        "from_name": "метанаблюдение",
        "to_name": "осознавание",
        "edge_type": "ENABLES",
        "explanation": "Метанаблюдение ведет к осознаванию",
        "confidence": 0.55,
        "metadata": {
          "source": "causal_chains"
        }
      }
    ],
    "metadata": {
      "video_id": "Rxoj94WQpsQ",
      "total_nodes": 45,
      "total_edges": 78,
      "node_types": {
        "CONCEPT": 20,
        "PRACTICE": 10,
        "TECHNIQUE": 8,
        "EXERCISE": 5,
        "PATTERN": 2,
        "PROCESS_STAGE": 0
      },
      "edge_types": {
        "ENABLES": 15,
        "REQUIRES": 12,
        "LEADS_TO": 20,
        "RELATED_TO": 25,
        "IS_PRACTICE_FOR": 3,
        "IS_TECHNIQUE_FOR": 2,
        "IS_EXERCISE_FOR": 1
      },
      "weight_statistics": {
        "min_weight": 0.283,
        "max_weight": 0.55,
        "avg_weight": 0.364,
        "median_weight": 0.305
      },
      "sources": {
        "graph_entities": 20,
        "concept_hierarchy": 10,
        "causal_chains": 8,
        "prerequisites": 5,
        "case_studies": 2,
        "semantic_relationships": 34,
        "cooccurrence": 6
      }
    }
  }
}
```

---

## Файлы Knowledge Graph

Для каждого обработанного видео создаются файлы:

```
data/sag_final/{year}/{month}/
├── {date}_{video_id}_{title}.for_vector.json      # Основной файл (с knowledge_graph внутри)
├── {date}_{video_id}_{title}.knowledge_graph.json  # Отдельный файл графа
└── {date}_{video_id}_{title}.for_review.md        # Markdown для просмотра
```

**Файл `*.knowledge_graph.json`** содержит только структуру графа (nodes, edges, metadata) для удобного использования в AI-боте или других приложениях.

---

## Использование Knowledge Graph

### Загрузка графа из JSON

```python
import json
from pathlib import Path

# Загрузить из основного файла
with open("data/sag_final/2025/06/video.for_vector.json", "r", encoding="utf-8") as f:
    data = json.load(f)
    knowledge_graph = data["knowledge_graph"]

# Или загрузить из отдельного файла графа
with open("data/sag_final/2025/06/video.knowledge_graph.json", "r", encoding="utf-8") as f:
    knowledge_graph = json.load(f)
```

### Использование API методов

```python
from text_processor.sarsekenov_processor import SarsekenovProcessor

processor = SarsekenovProcessor()

# Загрузить граф
with open("data/sag_final/2025/06/video.for_vector.json", "r", encoding="utf-8") as f:
    data = json.load(f)
    kg = data["knowledge_graph"]

# 1. Поиск практик для концепта/симптома
practices = processor.find_practices_for_concept("обида", kg)
for p in practices:
    print(f"Практика: {p['practice']}")
    print(f"Связь: {p['relation']}")
    print(f"Объяснение: {p.get('explanation', '')}")

# 2. Получение цепочки между концептами
chain = processor.get_concept_chain("страдание", "освобождение", kg)
if chain:
    print(f"Путь от '{chain['from_concept']}' к '{chain['to_concept']}':")
    for step in chain['steps']:
        print(f"  {step['from']} --[{step['relation']}]--> {step['to']}")

# 3. Рекомендация упражнения для практики
exercise = processor.recommend_exercise_for_practice("метанаблюдение", kg, "5 минут")
if exercise:
    print(f"Упражнение: {exercise['exercise']}")
    print(f"Длительность: {exercise['duration']}")
    print(f"Инструкции: {exercise['instructions']}")
```

### Интеграция в AI-бота

```python
def get_recommendations_for_symptom(symptom: str, knowledge_graph: Dict):
    """
    Получить рекомендации для работы с симптомом.
    
    Args:
        symptom: Описание симптома (напр. "захват внимания", "обида")
        knowledge_graph: Данные Knowledge Graph
        
    Returns:
        Dict с практиками, упражнениями и reasoning chains
    """
    processor = SarsekenovProcessor()
    
    # Найти практики
    practices = processor.find_practices_for_concept(symptom, knowledge_graph)
    
    # Для каждой практики найти упражнения
    recommendations = []
    for practice_info in practices:
        practice_name = practice_info['practice']
        exercise = processor.recommend_exercise_for_practice(
            practice_name, 
            knowledge_graph
        )
        
        recommendations.append({
            "symptom": symptom,
            "practice": practice_name,
            "relation": practice_info['relation'],
            "explanation": practice_info.get('explanation', ''),
            "exercise": exercise
        })
    
    return recommendations
```

---

## Интеграция

Knowledge Graph автоматически строится при обработке каждого видео:

1. Обработка блоков экстракторами SAG v2.0
2. Извлечение граф-сущностей (rule-based)
3. Построение Knowledge Graph из всех данных через метод `_build_knowledge_graph_from_blocks()`
4. Сохранение в JSON файл (в основном файле и отдельно)
5. Индексация в Vector DB

**Метод `_build_knowledge_graph_from_blocks()`** объединяет данные из всех источников:
- `graph_entities` → узлы типа CONCEPT, PRACTICE
- `concept_hierarchy` → иерархические связи
- `causal_chains` → узлы типа PROCESS_STAGE и связи EMERGES_FROM
- `prerequisites` → связи типа REQUIRES
- `case_studies` → связи с практиками
- `semantic_relationships` → автоматические связи RELATED_TO
- `cooccurrence` → перекрестные связи между блоками

**Вычисление весов:**
- Перед созданием связей система анализирует все блоки через `GraphWeightCalculator`
- Для каждой связи вычисляется комбинированный вес на основе PMI, co-occurrence и расстояния
- Все веса сохраняются в поле `confidence` каждой связи
- Статистика весов добавляется в `metadata.weight_statistics`

---

## Результат

Теперь каждый обработанный видео содержит:

- ✅ Полный Knowledge Graph со всеми взаимосвязями
- ✅ Типизированные узлы и связи
- ✅ Вычисляемые веса связей (PMI, co-occurrence, distance)
- ✅ Статистика весов в метаданных (min, max, avg, median)
- ✅ Контекст для каждого узла
- ✅ Готовые API методы для AI-бота
- ✅ Индексация в Vector DB для семантического поиска

**Максимально полная база знаний для супер-умного AI-бота психолога!** 🧠✨

---

## Навигация

- [Назад к README](../README.md)
- [SAG v2.0 Обзор](./sag-v2-overview.md)
- [Система экстракторов](./extractors-system.md)
- [API и интеграция](./api-integration.md)
- [Векторная база данных](./vector-database.md)

