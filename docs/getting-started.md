# 🚀 Полное руководство по быстрому старту

## Навигация

- [Назад к README](../README.md)
- [Детальная настройка](./configuration.md)
- [Архитектура](./architecture.md)
- [Устранение неполадок](#решение-типичных-проблем)

---

## 📖 Описание и назначение

**Назначение документа**: Это руководство предназначено для быстрого начала работы с Voice Bot Pipeline. Документ содержит пошаговые инструкции для установки, настройки и первого запуска системы.

**Для кого**: Документ предназначен для разработчиков, которые хотят быстро начать использовать систему для обработки YouTube-лекций и подготовки данных для AI-ботов.

**Что содержит**:
- Детальная настройка окружения (Python, виртуальное окружение, зависимости)
- Полная настройка API ключей и моделей (OpenAI, YouTube API, все варианты конфигурации)
- Подготовка URL для обработки (форматы, структура файлов)
- Запуск pipeline (все варианты: полный пайплайн, одиночные URL, батч-режим)
- Первые шаги после установки (проверка работоспособности, тестовый запуск)
- Решение типичных проблем (ошибки установки, проблемы с API, настройкой окружения)

**Когда использовать**: Используйте этот документ при первом знакомстве с проектом, при установке системы на новом компьютере, или когда нужно быстро восстановить рабочее окружение.

---

## 🆕 Новые возможности SAG v2.0

- **Автоматическая классификация**: Типы блоков, эмоциональные тона, сложность
- **Граф-сущности**: Нормализация и агрегация для графов знаний
- **Морфологическая обработка**: Русский язык с правильными грамматическими формами
- **Умная маршрутизация**: Классификация по коллекциям с метрикой уверенности

---

## Шаг 1: Подготовка окружения

### Автоматическая настройка (рекомендуется)

```powershell
# Автоматическая настройка (рекомендуется)
.\scripts\setup_all.ps1
```

**Что делает скрипт:**
- Проверяет наличие Python 3.8+
- Создает виртуальное окружение `.venv`
- Устанавливает все зависимости
- Создает необходимые папки
- Проверяет доступность FFmpeg (при необходимости)

### Ручная настройка

Если автоматическая настройка не работает, выполните вручную:

```powershell
# Создание виртуального окружения
python -m venv .venv

# Активация окружения
.\.venv\Scripts\Activate.ps1

# Установка зависимостей
pip install -r requirements.txt

# Создание необходимых папок
New-Item -ItemType Directory -Force -Path data\raw_subtitles, data\sag_final, data\chromadb
```

---

## Шаг 2: Настройка API ключей и моделей

### Создание файла .env

Создайте файл `.env` в корне проекта:

```env
# ========================================
# ОСНОВНЫЕ НАСТРОЙКИ
# ========================================

# Ключ OpenAI API (ОБЯЗАТЕЛЬНО для текстовой обработки)
# Используется в text_processor/ для преобразования сырого текста в JSON
# Получить на: https://platform.openai.com/
OPENAI_API_KEY=sk-proj-ваш_ключ_здесь

# 🆕 Ключ для YouTube Data API v3 (ОБЯЗАТЕЛЬНО для полных метаданных)
# Получить на: https://console.cloud.google.com/apis/credentials
YOUTUBE_API_KEY=AIzaSyD7VKqPUiiJYI3XYy6nwYFwUcfZNblQU4w

# ========================================
# МОДЕЛИ ДЛЯ ОБРАБОТКИ ТЕКСТА (OpenAI)
# ========================================

# Основная модель для разбивки на блоки
# Рекомендуется: gpt-4o-mini (быстро + дешево)
# Альтернативы: gpt-4o (медленно, дорого, отличное качество), gpt-4-turbo (компромисс)
PRIMARY_MODEL=gpt-4o-mini

# Модель для полировки результатов (опционально)
# Рекомендуется: gpt-5-mini (дешевле чем gpt-4)
# Оставьте пустым для отключения полировки
REFINE_MODEL=gpt-5-mini

# Задержка между запросами к OpenAI API (в секундах)
# По умолчанию: 1.0 секунда
# Увеличьте до 2.0-3.0 при частых ошибках 429 (Too Many Requests)
OPENAI_API_DELAY=1.0

# ========================================
# МОДЕЛИ ДЛЯ ЭМБЕДДИНГОВ (Sentence-Transformers)
# ========================================

# Провайдер эмбеддингов
MODEL_EMBEDDING=sentence-transformers

# Модель Sentence-Transformers для векторизации
# Рекомендуется: intfloat/multilingual-e5-large (лучшее качество для русского языка)
# Альтернативы:
#   - paraphrase-multilingual-mpnet-base-v2: баланс скорости/качества, 768 dim
#   - paraphrase-multilingual-MiniLM-L12-v2: быстрая модель, 384 dim
SENTENCE_TRANSFORMERS_MODEL=intfloat/multilingual-e5-large

# Устройство для обработки (cuda/cpu/mps или пусто для автоопределения)
# Автоматически использует GPU если доступен, иначе CPU
# SENTENCE_TRANSFORMERS_DEVICE=cuda  # Раскомментируйте для принудительного использования GPU

# ========================================
# ДОПОЛНИТЕЛЬНЫЕ НАСТРОЙКИ (опционально)
# ========================================

# Для Neo4j (если планируете граф-БД)
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=password

# Google Cloud (если нужно)
GOOGLE_CLOUD_PROJECT=your-project-id
```

### Получение API ключей

#### OpenAI API ключ

1. Перейдите на https://platform.openai.com/
2. Войдите или создайте аккаунт
3. Перейдите в раздел "API keys"
4. Создайте новый ключ
5. Скопируйте ключ в `.env` файл

#### YouTube Data API v3 ключ

1. Перейдите на https://console.cloud.google.com/
2. Создайте новый проект или выберите существующий
3. Включите YouTube Data API v3
4. Перейдите в "Credentials" → "Create Credentials" → "API Key"
5. Скопируйте ключ в `.env` файл

### Варианты настройки моделей

#### Вариант 1: Быстро и дешево (рекомендуется для начала)

```env
PRIMARY_MODEL=gpt-4o-mini
REFINE_MODEL=
```

#### Вариант 2: Качество и скорость

```env
PRIMARY_MODEL=gpt-4o-mini
REFINE_MODEL=gpt-5-mini
```

#### Вариант 3: Максимальное качество

```env
PRIMARY_MODEL=gpt-4o
REFINE_MODEL=gpt-4
```

---

## Шаг 3: Подготовка URL

### Добавление ссылок в urls.txt

Добавьте ссылки в файл `urls.txt` (по одной на строку):

```text
https://youtu.be/R39dqh5wA9U
https://youtube.com/watch?v=VIDEO_ID_2
https://youtube.com/live/LIVE_ID_3
```

### Поддерживаемые форматы URL

- `https://youtu.be/VIDEO_ID`
- `https://youtube.com/watch?v=VIDEO_ID`
- `https://youtube.com/live/LIVE_ID`
- `https://www.youtube.com/watch?v=VIDEO_ID`

---

## Шаг 4: Запуск

### Активация окружения

```powershell
# Активация окружения
.\.venv\Scripts\Activate.ps1
```

### Варианты запуска

#### Вариант 1: Полный пайплайн (рекомендуется)

```powershell
# 🆕 SAG v2.0 Запуск полного пайплайна
python pipeline_orchestrator.py --config config.yaml
```

#### Вариант 2: Прямая обработка с доменным процессором

```powershell
# 🆕 SAG v2.0 Прямая обработка с доменным процессором
python -m text_processor.sarsekenov_processor --input data/subtitles/VIDEO_ID.json --output data/sag_final --primary-model gpt-4o-mini --refine-model gpt-5-mini
```

#### Вариант 3: Только извлечение субтитров

```powershell
python subtitle_extractor/get_subtitles.py --url "ВАША_ССЫЛКА"
```

### Что происходит при запуске

1. **Чтение URL** из `urls.txt`
2. **Проверка реестра** на дубликаты - пропускает уже обработанные
3. **Получение метаданных** через YouTube API - название, канал, дата публикации
4. **Загрузка субтитров** с YouTube
5. **Сохранение файлов** с датами и названиями - структура `{year}/{month}/`
6. **SAG v2.0 Обработка** через доменный процессор:
   - Загружается полная граф-коллекция (442 узла + 259 отношений)
   - Автоматическая классификация блоков
   - Извлечение до 30 граф-сущностей с приоритизацией
   - Анализ семантических связей между концептами
   - Морфологическая обработка русского языка
   - Умная маршрутизация по коллекциям
7. **SAG v2.0 Сохранение результатов** в `data/sag_final/{year}/{month}/`:
   - JSON с расширенными метаданными (схема v2.0)
   - Граф-сущности на уровне документа
   - Семантические связи между концептами
   - Overview с валидацией длины (200+ символов)
   - Морфологические улучшения
8. **Запись в реестр** - история обработки, метрики, пути к файлам

---

## Первые шаги

### Проверка результатов

После успешного запуска проверьте созданные файлы:

```powershell
# Просмотр структуры результатов
Get-ChildItem -Recurse data\sag_final\2025\06\ | Select-Object Name, Length

# Просмотр JSON файла
Get-Content data\sag_final\2025\06\*.for_vector.json | ConvertFrom-Json | ConvertTo-Json -Depth 10
```

### Проверка логов

```powershell
# Просмотр логов в реальном времени
Get-Content pipeline.log -Wait

# Анализ ошибок
Select-String "ERROR" pipeline.log | Select-Object -Last 10

# Статистика выполнения
Select-String "Pipeline complete" pipeline.log | Measure-Object
```

---

## Решение типичных проблем

### 1. Ошибки API ключей

**Проблема**: `[ERROR] OPENAI_API_KEY не установлен`

**Решение**:
```powershell
# Проверка наличия файла .env
ls .env

# Проверка содержимого
Get-Content .env

# Проверка загрузки в Python
python -c "import os; from env_utils import load_env; load_env(); print(os.getenv('OPENAI_API_KEY'))"
```

**Возможные причины**:
- Файл `.env` отсутствует или в неправильном месте
- Неправильный формат ключа
- Проблемы с кодировкой файла

### 2. Проблемы с субтитрами

**Проблема**: `[ERROR] Не удалось получить субтитры`

**Решение**:
```powershell
# Проверка URL вручную
python subtitle_extractor\get_subtitles.py --url "ВАША_ССЫЛКА"

# Попробовать другой язык
python subtitle_extractor\get_subtitles.py --url "ВАША_ССЫЛКА" --language en
```

**Возможные причины**:
- Видео не имеет субтитров
- Видео приватное или удаленное
- Неправильный формат URL
- Блокировка по региону

### 3. Ошибки обработки текста

**Проблема**: `[INFO] Processing chunk X/Y` зависает или много ошибок `429 Too Many Requests`

**Решение**:
```powershell
# Проверка баланса OpenAI
# Зайти на https://platform.openai.com/usage

# Увеличение задержки между запросами к API (по умолчанию 1.0 секунда)
# Добавьте в .env:
OPENAI_API_DELAY=2.0  # Увеличьте до 2-3 секунд при частых ошибках 429

# Снижение нагрузки
# В .env изменить:
PRIMARY_MODEL=gpt-4o-mini
REFINE_MODEL=

# 🆕 SAG v2.0 Проверка отдельного блока
python -m text_processor.sarsekenov_processor --input data/subtitles/VIDEO_ID.json --output data/sag_final --primary-model gpt-4o-mini --refine-model gpt-5-mini
```

**Примечание**: Система автоматически добавляет задержку между запросами к OpenAI API для избежания rate limit ошибок. По умолчанию задержка составляет 1.0 секунду. Если вы видите много ошибок 429, увеличьте значение `OPENAI_API_DELAY` в `.env` файле до 2.0-3.0 секунд.

### 4. SAG v2.0 Специфические проблемы

**Проблема**: `too many values to unpack (expected 2)`

**Решение**:
```powershell
# Это исправлено в ETAP 5 - обновите код
# Проблема была в распаковке кортежей
# Теперь есть защита от ошибок
```

**Проблема**: `overview_summary слишком короткий`

**Решение**:
```powershell
# В ETAP 5 добавлена валидация длины
# Автоматический fallback на морфологию
# Минимум 200 символов гарантирован
```

**Проблема**: Граф-сущности не извлекаются

**Решение**:
```powershell
# В ETAP 5 добавлена агрегация на уровне документа
# Проверьте поле graph_entities в JSON
# Должно быть 12+ сущностей для качественных данных
```

### 5. Проблемы с зависимостями

**Проблема**: `ModuleNotFoundError: No module named 'XXX'`

**Решение**:
```powershell
# Переустановка окружения
Remove-Item -Recurse -Force .venv
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt

# Проверка установки
pip list | Select-String "openai|tiktoken|orjson"
```

### 6. Проблемы с PowerShell

**Проблема**: Ошибки выполнения скриптов

**Решение**:
```powershell
# Разрешение выполнения скриптов (от администратора)
Set-ExecutionPolicy RemoteSigned

# Альтернатива - прямые команды
python pipeline_orchestrator.py --config config.yaml
```

---

## Отладочные команды

### Полная диагностика системы

```powershell
# Полная диагностика системы
python -c "
import sys
print('Python:', sys.version)
print('Path:', sys.executable)

try:
    import openai, tiktoken, orjson
    print('Dependencies: OK')
except Exception as e:
    print('Dependencies error:', e)

import os
from pathlib import Path
print('.env exists:', Path('.env').exists())
print('config.yaml exists:', Path('config.yaml').exists())
print('urls.txt exists:', Path('urls.txt').exists())

from env_utils import load_env
load_env()
print('API key loaded:', bool(os.getenv('OPENAI_API_KEY')))
"
```

### Проверка доступности YouTube

```powershell
# Проверка доступности YouTube
python -c "
from subtitle_extractor.get_subtitles import YouTubeSubtitlesExtractor
extractor = YouTubeSubtitlesExtractor('temp')
result = extractor.extract_video_id('https://youtu.be/dQw4w9WgXcQ')
print('YouTube parsing:', 'OK' if result else 'FAILED')
"
```

### Тест OpenAI API

```powershell
# Тест OpenAI API
python -c "
from openai import OpenAI
try:
    client = OpenAI()
    response = client.chat.completions.create(
        model='gpt-4o-mini',
        messages=[{'role': 'user', 'content': 'test'}],
        max_tokens=5
    )
    print('OpenAI API: OK')
except Exception as e:
    print('OpenAI API error:', e)
"
```

---

## Логи и мониторинг

### Просмотр логов

```powershell
# Просмотр логов в реальном времени
Get-Content pipeline.log -Wait

# Анализ ошибок
Select-String "ERROR" pipeline.log | Select-Object -Last 10

# Статистика выполнения
Select-String "Pipeline complete" pipeline.log | Measure-Object

# Проверка размеров результатов
Get-ChildItem data\sag_final\*.json | Select-Object Name, Length | Sort-Object Length
```

---

## Следующие шаги

После успешного запуска:

1. **Изучите результаты** - проверьте созданные JSON и Markdown файлы
2. **Настройте конфигурацию** - см. [Детальная настройка](./configuration.md)
3. **Изучите архитектуру** - см. [Архитектура](./architecture.md)
4. **Настройте векторную БД** - см. [Векторная база данных](./vector-database.md)
5. **Изучите API** - см. [API и интеграция](./api-integration.md)

---

## Навигация

- [Назад к README](../README.md)
- [Детальная настройка](./configuration.md)
- [Архитектура](./architecture.md)
- [Векторная база данных](./vector-database.md)
- [API и интеграция](./api-integration.md)

