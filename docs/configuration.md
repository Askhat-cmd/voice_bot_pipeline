# ⚙️ Детальная настройка

## Навигация

- [Назад к README](../README.md)
- [Быстрый старт](./getting-started.md)
- [Архитектура](./architecture.md)

---

## 📖 Описание и назначение

**Назначение документа**: Это полное руководство по настройке Voice Bot Pipeline. Документ содержит детальное описание всех параметров конфигурации, переменных окружения и режимов работы системы.

**Для кого**: Документ предназначен для разработчиков, которые хотят настроить систему под свои нужды, для системных администраторов, развертывающих систему, и для пользователей, которым нужны продвинутые настройки.

**Что содержит**:
- Конфигурация config.yaml (полная, все секции: логирование, пайплайн, SAG v2.0, пути, векторная БД)
- Переменные окружения (.env) - подробно (все опции: API ключи, модели, параметры обработки)
- Доменные режимы обработки (все режимы: sarsekenov, generic, настройка режимов)
- Настройка путей (все пути к файлам и папкам, кастомизация структуры)
- Параметры обработки (настройки блоков, метаданных, экстракторов)
- Настройка векторной БД (ChromaDB, Sentence-Transformers, коллекции)
- Оптимизация производительности (настройки для ускорения обработки)

**Когда использовать**: Используйте этот документ при настройке системы под конкретные требования, при развертывании в production, при оптимизации производительности, или когда нужно изменить стандартные настройки.

---

## Конфигурация `config.yaml`

```yaml
# ========================================
# НАСТРОЙКИ ЛОГИРОВАНИЯ
# ========================================
logging:
  level: "INFO"          # DEBUG, INFO, WARNING, ERROR
  log_file: "pipeline.log"

# ========================================
# НАСТРОЙКИ ПАЙПЛАЙНА
# ========================================
pipeline:
  # 🆕 SAG v2.0 Настройки
  schema_version: "2.0"
  processing_version: "v2.1"
  
  # Настройки извлечения субтитров
  subtitles:
    output_dir: "data/raw_subtitles"
    language: "ru"         # Предпочитаемый язык
    
  # Настройки обработки текста (SAG v2.0)
  text_processing:
    output_dir: "data/sag_final"
    
  # Папка для результатов выполнения
  results_dir: "data/sag_final"
  
  # 🆕 Настройки Knowledge Graph
  knowledge_graph:
    enabled: true
    export_formats:
      - json
      - neo4j  # Опционально
    neo4j:
      uri: "bolt://localhost:7687"
      user: "neo4j"
      password: "password"
    reasoning:
      max_chain_depth: 5
      confidence_threshold: 0.7

# ========================================
# НАСТРОЙКИ ВЕКТОРНОЙ БД
# ========================================
vector_db:
  db_path: "data/chromadb"
  collection_prefix: "sag_v2"
  auto_index: false
  index_levels:
    - "documents"
    - "blocks"
    - "graph_entities"
    - "knowledge_graph"
  embedding:
    provider: "sentence-transformers"
    model: "intfloat/multilingual-e5-large"
    dimension: 1024
  batch_size: 100
```

---

## Переменные окружения (.env) - подробно

### ОБЯЗАТЕЛЬНЫЕ НАСТРОЙКИ

```env
# OpenAI API ключ - получить на https://platform.openai.com/
OPENAI_API_KEY=sk-proj-...

# 🆕 Ключ для YouTube Data API v3 (ОБЯЗАТЕЛЬНО для полных метаданных)
# Получить на: https://console.cloud.google.com/apis/credentials
YOUTUBE_API_KEY=AIzaSyD7VKqPUiiJYI3XYy6nwYFwUcfZNblQU4w
```

### МОДЕЛИ (настройка производительности/стоимости)

```env
# Основная модель для структурирования текста
# Варианты:
# - gpt-4o-mini: быстро, дешево, хорошее качество (РЕКОМЕНДУЕТСЯ)
# - gpt-4o: медленно, дорого, отличное качество
# - gpt-4-turbo: компромисс скорость/качество
PRIMARY_MODEL=gpt-4o-mini

# Модель для финальной полировки (опционально)
# Варианты:
# - gpt-5-mini: дешево, хорошо для полировки (РЕКОМЕНДУЕТСЯ)
# - gpt-4: дорого, отлично для полировки
# - "" (пустая строка): отключить полировку
REFINE_MODEL=gpt-5-mini

# Модель для создания эмбеддингов (векторизация)
# Используется Sentence-Transformers для локальной генерации эмбеддингов
# Варианты:
# - intfloat/multilingual-e5-large: лучшее качество для русского языка, 1024 dim (РЕКОМЕНДУЕТСЯ)
# - paraphrase-multilingual-mpnet-base-v2: баланс скорости/качества, 768 dim
# - paraphrase-multilingual-MiniLM-L12-v2: быстрая модель, 384 dim
MODEL_EMBEDDING=sentence-transformers
SENTENCE_TRANSFORMERS_MODEL=intfloat/multilingual-e5-large

# Устройство для Sentence-Transformers (cuda/cpu/mps или пусто для автоопределения)
# Автоматически использует GPU если доступен, иначе CPU
# SENTENCE_TRANSFORMERS_DEVICE=cuda  # Раскомментируйте для принудительного использования GPU

# Задержка между запросами к OpenAI API (в секундах)
# По умолчанию: 1.0 секунда
# Увеличьте до 2.0-3.0 при частых ошибках 429 (Too Many Requests)
OPENAI_API_DELAY=1.0
```

### ПРОДВИНУТЫЕ НАСТРОЙКИ (опционально)

```env
# Кастомные промпты (если нужна тонкая настройка)
MAIN_PROMPT="""Ты — редактор лекций. Разбей текст на крупные смысловые блоки..."""
REFINE_PROMPT="""Улучши заголовки и описания блоков..."""
FALLBACK_PROMPT="""Блок создан автоматически из-за ошибки обработки."""

# Настройки для граф-БД Neo4j
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=your_password

# Google Cloud (для дополнительных сервисов)
GOOGLE_CLOUD_PROJECT=your-project-id
```

---

## Доменные режимы обработки

### Что такое домен?

**Домен** — это специализированный профиль обработки текста, настроенный под конкретную тематику или автора.

### Доступные домены

- **`sarsekenov`** (по умолчанию) — нейросталкинг/неосталкинг Саламата Сарсекенова
- **`generic`** — универсальная обработка без доменной специфики

### Настройка домена

```powershell
# В pipeline_orchestrator.py
python pipeline_orchestrator.py --config config.yaml --domain sarsekenov
```

---

## Настройка путей

Все пути настраиваются в `config.yaml`:

```yaml
pipeline:
  subtitles:
    output_dir: "data/raw_subtitles"  # Путь для субтитров
  text_processing:
    output_dir: "data/sag_final"      # Путь для результатов SAG v2.0
  results_dir: "data/sag_final"      # Путь для результатов выполнения

vector_db:
  db_path: "data/chromadb"            # Путь для векторной БД
```

---

## Навигация

- [Назад к README](../README.md)
- [Быстрый старт](./getting-started.md)
- [Архитектура](./architecture.md)
- [Структура данных](./data-structure.md)

