# 🏗 Архитектура и компоненты

## Навигация

- [Назад к README](../README.md)
- [Структура проекта](#структура-проекта)
- [Система экстракторов](./extractors-system.md)
- [Конфигурация](./configuration.md)

---

## 📖 Описание и назначение

**Назначение документа**: Это полная документация по архитектуре Voice Bot Pipeline. Документ описывает все компоненты системы, их взаимодействие, схему работы и структуру проекта.

**Для кого**: Документ предназначен для разработчиков, которые хотят понять архитектуру системы, для новых участников проекта, изучающих код, и для архитекторов, планирующих изменения или расширения функциональности.

**Что содержит**:
- Схема работы (полный flow от YouTube URL до JSON для векторной БД)
- Ключевые компоненты (все модули: Subtitle Extractor, Text Processor, Vector Database, Pipeline Orchestrator)
- Архитектура обработки (детальная схема с SAG v2.0, экстракторами, эмбеддингами)
- Структура проекта (детальная структура всех папок и файлов, назначение каждого модуля)
- Взаимодействие компонентов (как компоненты работают вместе, потоки данных)
- Технологический стек (все используемые библиотеки и инструменты)

**Когда использовать**: Используйте этот документ при изучении кодовой базы, при планировании новых функций, при рефакторинге, или когда нужно объяснить архитектуру системы другим разработчикам.

---

## Схема работы

```
YouTube URL → Субтитры → Семантические блоки → JSON для векторной БД
     ↓              ↓                ↓                    ↓
1. Извлечение    2. Чистка      3. Структура      4. Метаданные
   субтитров       текста         + таймкоды        + ID + ссылки
```

---

## Ключевые компоненты

### 1. **Subtitle Extractor** (`subtitle_extractor/`)

**Назначение**: Извлечение субтитров с YouTube

**Технология**: `youtube-transcript-api`

**Поддерживаемые форматы**:
- Обычные видео: `youtube.com/watch?v=ID`
- Короткие ссылки: `youtu.be/ID`
- Live-трансляции: `youtube.com/live/ID`

**Выходные форматы**: JSON (структурированный), SRT (стандартный), TXT (чистый текст)

---

### 2. **Text Processor** (`text_processor/`)

**Назначение**: Преобразование субтитров в семантические блоки с SAG v2.0

**Доступные процессоры**:
- `sarsekenov_processor.py` — **SAG v2.0 доменный** (нейросталкинг/неосталкинг)
- `subtitles_to_blocks.py` — универсальный

**ИИ-модели**: OpenAI GPT (настраиваемые через .env)

**SAG v2.0 возможности**:
- Автоматическая классификация блоков
- Извлечение граф-сущностей (442 узла + 259 отношений)
- Морфологическая обработка русского языка
- Умная маршрутизация по коллекциям

**Система экстракторов знаний (5 этапов)**:
- **Этап 1**: `TerminologyValidator` — фильтр качества терминов
- **Этап 2**: `NeurostalkingPatternExtractor` — детектор паттернов
- **Этап 3**: `CausalChainExtractor` — причинно-следственные цепочки
- **Этап 4**: `ConceptHierarchyExtractor` — иерархия концептов
- **Этап 5**: `SarsekenovProcessor` — оркестратор (объединяет всё)

**Дополнительные экстракторы** (LLM-based модули):
- `safety_extractor.py` — Информация о безопасности и противопоказаниях
- `causal_chain_extractor.py` — Причинно-следственные цепочки процессов
- `concept_hierarchy_extractor.py` — Иерархия концептов
- `case_study_extractor.py` — Структурированные кейсы применения практик
- `prerequisite_extractor.py` — Предпосылки и последовательность обучения

**Вычисление весов связей** (GraphWeightCalculator):
- `graph_weight_calculator.py` — Автоматический расчет весов связей в Knowledge Graph
  - PMI (Pointwise Mutual Information) — статистическая мера взаимосвязи
  - Co-occurrence frequency — частота совместной встречаемости
  - Distance weight — близость концептов в тексте
  - Комбинированный вес (40% frequency + 30% PMI + 30% distance)
  - Статистика весов в метаданных графа (min, max, avg, median)

**Где сохраняются данные экстракторов**: Прямо в блоки финального JSON (`data/sag_final/{year}/{month}/*.for_vector.json`)

---

### 3. **Vector Database** (`vector_db/`)

**Назначение**: Семантический поиск по обработанным данным

**Технология**: ChromaDB + Sentence-Transformers

**Модель эмбеддингов**: `intfloat/multilingual-e5-large` (1024 dim, локальная генерация)

**Возможности**:
- Многоуровневая индексация (документы, блоки, граф-сущности)
- Семантический поиск по смыслу
- Гибридный поиск по всем коллекциям
- Фильтрация по метаданным

**Преимущества**: Нет затрат на API, нет rate limits, локальная обработка

---

### 4. **Pipeline Orchestrator** (`pipeline_orchestrator.py`)

**Назначение**: Координация всего процесса

**Функции**: Логирование, обработка ошибок, батч-режим, опциональная индексация в векторную БД

**Конфигурация**: YAML-файл с настройками

---

## Архитектура обработки

```
YouTube URL
    ↓
[Subtitle Extractor] → Сырые субтитры (JSON/SRT/TXT)
    ↓
[Text Processor + OpenAI GPT] → Структурированные блоки SAG v2.0 (JSON)
    ↓
[Система экстракторов знаний (5 этапов)] → Глубокая структуризация
    ├── Этап 1: TerminologyValidator → Фильтр качества терминов
    ├── Этап 2: NeurostalkingPatternExtractor → Детектор паттернов
    ├── Этап 3: CausalChainExtractor → Причинно-следственные цепочки
    ├── Этап 4: ConceptHierarchyExtractor → Иерархия концептов
    └── Этап 5: SarsekenovProcessor → Оркестрация и объединение
    ↓
[SAG v2.0 Граф-коллекция] → Автоматическое извлечение (параллельно)
    ├── Граф-сущности (442 узла)
    ├── Семантические связи (259 отношений)
    └── Нормализация и агрегация
    ↓
[Дополнительные LLM-экстракторы] → Расширенные данные
    ├── Safety Extractor → Информация о безопасности
    ├── Causal Chain Extractor → Причинно-следственные цепочки
    ├── Concept Hierarchy Extractor → Иерархия концептов
    ├── Case Study Extractor → Структурированные кейсы
    └── Prerequisite Extractor → Предпосылки и последовательность
    ↓
[Sentence-Transformers] → Эмбеддинги (1024 dim, локально)
    ↓
[ChromaDB] → Векторная база данных (семантический поиск)
```

**Важно:** 
- **OpenAI API** используется для текстовой обработки и работы экстракторов
- **Sentence-Transformers** используется для векторизации (локальная генерация эмбеддингов)
- **SAG v2.0 Граф-коллекция** работает автоматически (rule-based, без API)
- **Система экстракторов (1-5)** работает последовательно через LLM
- **Данные экстракторов** сохраняются прямо в блоки финального JSON файла

---

## Структура проекта

```
voice_bot_pipeline/
├── 📁 subtitle_extractor/      # Модуль извлечения субтитров
│   ├── 🐍 get_subtitles.py     # Основной скрипт загрузки
│   └── 📁 scripts/             # Вспомогательные скрипты
│
├── 📁 text_processor/          # Обработка текста
│   ├── 🐍 sarsekenov_processor.py  # SAG v2.0 доменный процессор
│   ├── 🐍 subtitles_to_blocks.py   # Универсальный процессор
│   ├── 📁 extractors/         # Система экстракторов (5 этапов)
│   ├── 📁 validators/         # Валидаторы терминов
│   ├── 📁 orchestrator/        # Оркестратор и Knowledge Graph
│   │   ├── graph_weight_calculator.py  # Вычисление весов связей (PMI, co-occurrence, distance)
│   │   └── knowledge_graph.py  # Построение графа знаний
│   └── 📁 export/             # Экспорт для RAG
│
├── 📁 vector_db/              # Векторная база данных
│   ├── 🐍 chromadb_manager.py  # Управление ChromaDB
│   ├── 🐍 embedding_service.py # Генерация эмбеддингов
│   ├── 🐍 vector_indexer.py   # Индексация данных
│   └── 🐍 vector_search.py    # Семантический поиск
│
├── 📁 utils/                  # Утилиты
│   ├── 🐍 video_registry.py   # Реестр видео
│   ├── 🐍 youtube_metadata_fetcher.py  # Метаданные YouTube
│   └── 🐍 file_utils.py       # Работа с файлами
│
├── 📁 scripts/                # Вспомогательные скрипты
│   ├── 🐍 manage_registry.py  # Управление реестром
│   ├── 🐍 index_existing_files.py  # Индексация существующих файлов
│   └── 🐍 test_vector_search.py  # Тестирование поиска
│
├── 📁 tests/                  # Тесты
│   ├── 📁 extractors/         # Тесты экстракторов
│   ├── 📁 orchestrator/       # Тесты оркестратора
│   └── 📁 integration/        # Интеграционные тесты
│
├── 📁 data/                   # Данные
│   ├── 📁 raw_subtitles/      # Исходные субтитры
│   ├── 📁 sag_final/          # SAG v2.0 результаты
│   ├── 📁 chromadb/           # Векторная БД
│   └── video_registry.json    # Реестр видео
│
├── 🐍 pipeline_orchestrator.py  # Главный оркестратор
├── 📄 config.yaml             # Конфигурация
├── 📄 .env                    # Переменные окружения (не в Git)
└── 📄 README.md               # Документация
```

---

## Навигация

- [Назад к README](../README.md)
- [Система экстракторов](./extractors-system.md)
- [Конфигурация](./configuration.md)
- [Структура данных](./data-structure.md)
- [Векторная база данных](./vector-database.md)

